## Conversation
The user asked for an install.sh in the private dotfiles repository and wanted minimal references to that repository in the public dotfiles repository. The user then asked to commit the completed changes.

## Actions
Removed private directory handling from the public config, script, skill, and rule installers. Added one optional install-private Make target, invoked it after public installation from install-all and the root bootstrap, and updated CLAUDE.md. Ran shellfmt.sh, ShellCheck, diff checks, Make dry runs, and isolated public-only and combined installation tests. Committed the private entry point separately as 3b0034c. Created this commit with signing and sign-off enabled and hooks disabled so nothing was published.

## Errors & Mistakes
The first isolated installer test did not run because the command guard rejected an inline rm cleanup step. Reran the test with recoverable cleanup through the macOS trash command. The first shellfmt.sh invocation accepted multiple paths but processed only the first path, so each remaining script was formatted and checked in a loop.

## Dead Ends
None.

## Hints for Future Agents
Keep private installation behavior in dotfilesprivate/install.sh. The public component installers should remain private-repository agnostic. The Makefile retains separate private clone and pull lifecycle targets.

## Codebase Discoveries
The root install.sh is the GitHub Codespaces bootstrap and intentionally installs public configs and scripts rather than optional Go tools. It now calls the single optional private handoff afterward.

## Open Questions
None.
